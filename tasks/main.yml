---
- name: Install some common editors
  become: true
  apt:
    name:
      - emacs
      - vim
      - nano
      - evince

- name: install basic .vimrc
  copy:
    src: .vimrc
    dest: "${HOME}"
